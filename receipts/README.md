# Receipts

Each public demo creates a new timestamped directory under `receipts/runs/`.
It contains the exact model identity, per-fixture JSON receipts, hash-chained
journals, harness evidence, and a `summary.json` with receipt hashes.

The generated run directories are ignored by Git. Do not believe the bundled
description: run the demos and inspect the receipts produced on your machine.
